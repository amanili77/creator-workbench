# 安全与隐私

## 本地数据

工作台会在 `用户数据/`（或 `WORKBENCH_USER_DATA` 指定的目录）保存个人资料、数据库、备份、API Key 和第三方登录状态。该目录不应进入 Git、网盘公开链接或问题截图。

API Key 在 Windows 上使用当前账户的 DPAPI 加密。非 Windows 环境中的兼容实现仅用于保证功能可运行，不应视为系统级安全存储；建议通过受保护的本地环境使用。

应用默认仅监听 `127.0.0.1`。不要把服务直接暴露到公网；项目当前不包含公网部署所需的身份验证、CSRF 防护和访问控制。

## 报告问题

请通过 GitHub Security Advisory 私下报告可能导致密钥、Cookie、个人资料泄露或本地文件越权访问的问题。公开 Issue 中不要粘贴真实凭据、`用户数据/` 内容或完整日志。

## 凭据意外泄露

如果凭据曾提交到 Git 历史：

1. 立即在对应服务商处撤销或轮换凭据。
2. 停止推送和分享仓库。
3. 清理整个 Git 历史，而不只是删除当前文件。
4. 重新克隆清理后的仓库并再次进行敏感信息扫描。
